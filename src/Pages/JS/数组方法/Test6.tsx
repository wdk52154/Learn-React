import React from 'react';

//迭代：用于描述按顺序访问集合中的每个元素的过程。
const Test6: React.FC = () => {
  //1 forEach 遍历数组中的元素.没有返回值,不会修改原数组
  //   const arr = [1, 2, 3, 4, 5];
  //   const list = arr.forEach((v, k) => {
  //     console.log(v, k);
  //   });
  //   console.log(list);

  //2 map 遍历数组中的元素.返回一个新数组,不会修改原数组.没有return,会返回undefined
  //   const arr = [1, 2, 3, 4, 5];
  //   const list = arr.map((v, k) => {
  //     console.log(v, k);
  //   });
  //   console.log(list);

  return <></>;
};

export default Test6;
